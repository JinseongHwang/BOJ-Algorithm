#include <iostream>
using namespace std;
void init() {
	ios_base::sync_with_stdio(0);
	cin.tie(0); cout.tie(0);
}
int main() {
	init();
	int a, b, c; cin >> a >> b >> c;
	if (b >= c) {
		cout << "-1\n"; 
	}
	else {
		cout << a / (c - b) + 1 << "\n";
	}
	return 0;
}
